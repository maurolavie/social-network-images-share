// Este index va a servir como indice de todos los modelos que vamos a tener en la carpeta models
// Va a tener un objeto que va a exportar todos los modelos de la careta module
module.exports = {
    Image: require('./image'),
    Comment: require('./comment')
}